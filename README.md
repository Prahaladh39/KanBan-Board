### `subtract(a, b)`

Subtracts the second number from the first.

**Parameters:**
- `a` (number): The number to subtract from (minuend).
- `b` (number): The number to subtract (subtrahend).

**Returns:**
(number): The result of the subtraction (`a - b`).