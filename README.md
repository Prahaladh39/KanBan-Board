```markdown
### `add(a, b, c)`

Adds three numbers together.

**Parameters:**
- `a` (Number): The first number.
- `b` (Number): The second number.
- `c` (Number): The third number.

**Returns:**
- (Number): The sum of `a`, `b`, and `c`.
```