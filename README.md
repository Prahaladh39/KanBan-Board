```markdown
## Functions

### `add(userId: string, includePosts: boolean)`
Adds a new user to the system.

### `subtract(a, b)`
Calculates the sum of two numbers `a` and `b`, and returns that sum plus 10.
```